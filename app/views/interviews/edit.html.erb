<%= turbo_frame_tag dom_id(@interview) do %>
  <div class="mt-6 rounded-md border border-slate-200 bg-white p-4">
    <h2 class="text-lg font-semibold text-slate-900">Edit interview</h2>
    <%= render "shared/form_errors", object: @interview %>
    <%= render "form" %>
  </div>
<% end %>
